{
  "name": "question-bank-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"python -m uvicorn backend.main:app --app-dir .. --reload --port 8000\" --names \"vite,api\" --prefix-colors \"cyan,yellow\"",
    "dev:vite": "vite",
    "dev:api": "python -m uvicorn backend.main:app --app-dir .. --reload --port 8000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "vite": "^5.3.1"
  }
}
