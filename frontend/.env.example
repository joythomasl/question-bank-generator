# Frontend environment variables (local dev only)
# Copy this file to .env and fill in your key.
# This file is safe to commit — .env is gitignored.

# A SECOND, SEPARATE NVIDIA NIM key (do NOT reuse the pipeline's NVIDIA_API_KEY)
# Get one at: https://build.nvidia.com/
NVIDIA_API_KEY_LIVE=
